# PHP Workspace

This directory is reserved for PHP-side bundle shaping, response generation, and other server-side support for `loadBundle`.

Keep browser/runtime loader code in `src/` and use this area for PHP-only implementation details.
